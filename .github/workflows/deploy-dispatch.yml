name: Deploy Dispatch Rules (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch rules retired
        run: |
          echo "App Engine dispatch rules are no longer deployed."
          echo "Ananya Boutique production uses Firebase App Hosting for frontends and Cloud Run for the API."
